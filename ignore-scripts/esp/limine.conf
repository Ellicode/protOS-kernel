# Timeout in seconds that Limine will use before automatically booting.
timeout: 0
/protOS
    protocol: limine
    kernel_path: boot():/kernel.elf
    module_path: boot():/boot/initramfs.tar
    # resolution: 1920x1080x32
    resolution: 800x600x32
    # resolution: 1080x720x32
